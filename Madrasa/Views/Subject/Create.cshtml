@model Madrasa.Models.Subject

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Subject</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.titleList)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.titleList)
            @Html.ValidationMessageFor(model => model.titleList)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ancestorsIds)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ancestorsIds)
            @Html.ValidationMessageFor(model => model.ancestorsIds)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
