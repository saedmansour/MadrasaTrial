@model Madrasa.Models.SchoolClass

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Classs</legend>

    <div class="display-label">name</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.name)
    </div>

    <div class="display-label">schoolId</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.schoolId)
    </div>

    <div class="display-label">studentNumber</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.studentNumber)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
